/** Green find (search) limit bounds. */
export const SEARCH_LIMIT_MIN = 1;
export const SEARCH_LIMIT_MAX = 50;

/** Green recent (list) limit bounds. */
export const LIST_LIMIT_MIN = 1;
export const LIST_LIMIT_MAX = 100;
