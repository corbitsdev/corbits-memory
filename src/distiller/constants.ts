/** Stable generator id for the resident distiller — use with feed excludeGenerator. */
export const RESIDENT_DISTILLER_AGENT_ID = "resident-distiller";

/** Default workflow id when host does not override. */
export const RESIDENT_DISTILLER_WORKFLOW_ID = "resident-memory-distiller";
